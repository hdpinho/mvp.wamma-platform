# URL del backend (Spring Boot), sin barra final.
# Local: http://localhost:8080 · Vercel (Production): https://wamma-backend.onrender.com
# Si se deja vacía, el frontend funciona en modo maqueta, sin servidor.
VITE_API_URL=http://localhost:8080
